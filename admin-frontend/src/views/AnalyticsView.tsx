import { BarChart3 } from 'lucide-react';
import { PlaceholderView } from '@/components/PlaceholderView';

export function AnalyticsView() {
  return (
    <PlaceholderView
      icon={BarChart3}
      title="Analytics lands in Phase 5"
      description="Date-range/department/job filters, KPI strip, and the 4 chart types, all from the same shared candidates feed as Dashboard/Candidates."
    />
  );
}
